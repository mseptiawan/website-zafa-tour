<%- layout('layouts/main', { title }) %>

<div class=" min-h-screen bg-slate-50/50 p-8 antialiased selection:bg-blue-50 selection:text-blue-600">
  <div class="max-w-6xl mx-auto space-y-8">

    <!-- Page Header Komponen -->
    <%- include('../partials/page-header', {
      section: 'Area Kerja',
      module: 'Penugasan & Backup',
      title: 'Permintaan Delegasi Tugas',
      description: 'Konfirmasi kesediaan Anda untuk mem-backup serta menjaga kontinuitas tugas rekan kerja yang sedang cuti.'
    }) %>

    <!-- Stats Grid Komponen -->
    <%- include('../partials/_stats-grid', {
      items: [
        { 
          label: 'Permintaan Aktif', 
          value: delegations.length, 
          unit: 'Berkas', 
          icon: 'fa-solid fa-clock-rotate-left', 
          bgClass: 'bg-white border-slate-200/60', 
          iconBoxClass: 'bg-amber-50 text-amber-600 border-amber-100' 
        },
        { 
          label: 'Riwayat Konfirmasi', 
          value: historyDelegations.length, 
          unit: 'Berkas', 
          icon: 'fa-solid fa-circle-check', 
          bgClass: 'bg-white border-slate-200/60', 
          iconBoxClass: 'bg-blue-50 text-blue-600 border-blue-100' 
        },
        { 
          label: 'Total Keterlibatan Backup', 
          value: (delegations.length + historyDelegations.length), 
          unit: 'Total Tugas', 
          icon: 'fa-solid fa-briefcase', 
          bgClass: 'bg-gradient-to-br from-slate-900 to-slate-800 border-slate-800', 
          labelClass: 'text-slate-300', 
          valueClass: 'text-white', 
          unitClass: 'text-slate-400',
          iconBoxClass: 'bg-white/10 text-white border-transparent' 
        }
      ]
    }) %>

    <!-- Tab Navigation Komponen (Mewah Capsule Style) -->
    <%- include('../partials/_tab-nav', {
      tabs: [
        { id: 'active', label: `Permintaan Aktif (${delegations.length})` },
        { id: 'history', label: `Riwayat Delegasi (${historyDelegations.length})` }
      ]
    }) %>

    <!-- Tab Content Panes dengan Smooth Opacity Transition -->
    <div id="tab-content-active" class="tab-pane block opacity-100 transition-all duration-300 transform translate-y-0">
      <%- include('./partials/_delegation-table.ejs', { delegations: delegations, type: 'active' }) %>
    </div>

    <div id="tab-content-history" class="tab-pane hidden opacity-0 transition-all duration-300 transform translate-y-2">
      <%- include('./partials/_delegation-table.ejs', { delegations: historyDelegations, type: 'history' }) %>
    </div>

  </div>
</div>