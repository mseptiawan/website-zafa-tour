<div class="space-y-4">
  <div class="flex items-start gap-3">
    <div
      class="flex h-8 w-8 shrink-0 items-center justify-center rounded-lg border border-amber-200/50 bg-amber-50 text-amber-600"
    >
      <i class="fa-solid fa-gavel text-xs"></i>
    </div>
    <div>
      <h4 class="text-[12px] font-bold tracking-wider text-slate-900 uppercase">Panel Keputusan</h4>
      <p class="mt-0.5 text-[11px] text-slate-400">
        Tinjau kesesuaian anggaran operasional dinas sebelum memberikan keputusan.
      </p>
    </div>
  </div>

  <!-- FORM KEPUTUSAN MANAGEMENT -->
  <form
    id="approvalForm"
    method="POST"
    action="/trip/approval/<%= activeApproval._id %>"
    class="m-0 space-y-3.5"
  >
    <!-- Input hidden untuk menampung jenis aksi (APPROVE / REJECT) -->
    <input type="hidden" name="action" id="actionInput" value="" />

    <div>
      <label class="mb-1 block text-[9px] font-semibold tracking-widest text-slate-400 uppercase">
        Catatan Keputusan (Opsional)
      </label>
      <textarea
        name="notes"
        rows="3"
        class="w-full resize-none rounded-xl border border-slate-200/80 bg-slate-50/30 p-2.5 text-[12px] text-slate-800 placeholder-slate-400 transition outline-none focus:border-blue-500 focus:bg-white focus:ring-4 focus:ring-blue-500/10"
        placeholder="Berikan alasan jika menolak, atau arahan khusus jika menyetujui..."
      ></textarea>
    </div>

    <div class="grid grid-cols-2 gap-2.5 pt-1">
      <!-- BUTTON REJECT -->
      <button
        type="button"
        onclick="submitDecision('REJECT')"
        class="flex h-10 cursor-pointer items-center justify-center gap-1.5 rounded-xl border border-rose-200 bg-rose-50/30 text-xs font-semibold text-rose-700 transition-all hover:bg-rose-50 active:scale-[0.98]"
      >
        <i class="fa-solid fa-xmark text-[11px]"></i>
        Tolak Dinas
      </button>

      <!-- BUTTON APPROVE -->
      <button
        type="button"
        onclick="submitDecision('APPROVE')"
        class="flex h-10 cursor-pointer items-center justify-center gap-1.5 rounded-xl bg-emerald-600 text-xs font-semibold text-white shadow-xs transition-all hover:bg-emerald-700 active:scale-[0.98]"
      >
        <i class="fa-solid fa-check text-[11px]"></i>
        Setujui
      </button>
    </div>
  </form>
</div>

<script>
  function submitDecision(status) {
    const actionInput = document.getElementById("actionInput");
    const form = document.getElementById("approvalForm");

    if (actionInput && form) {
      actionInput.value = status;

      const notes = form.querySelector('textarea[name="notes"]').value.trim();
      if (status === "REJECT" && !notes) {
        alert("Harap masukkan alasan penolakan pada kolom catatan keputusan.");
        return;
      }

      form.submit();
    }
  }
</script>
