<%- layout('layouts/main', { title }) %>

  <div class="ml-72 min-h-screen bg-slate-50/50 p-8">

    <div class="max-w-5xl mx-auto">

      <!-- HEADER -->
      <div class="flex items-start justify-between mb-8">

        <div>
          <h1 class="text-2xl font-semibold text-slate-800">
            Daftar Pengajuan Dinas
          </h1>
          <p class="text-sm text-slate-500 mt-1">
            Kelola seluruh pengajuan perjalanan dinas
          </p>
        </div>

        <a href="/trip/request"
          class="bg-slate-900 hover:bg-slate-800 text-white text-sm px-4 py-3 rounded-xl transition">
          + Buat Pengajuan
        </a>

      </div>

      <!-- CARD -->
      <div class="bg-white border border-slate-200 rounded-2xl shadow-sm overflow-hidden">

        <div class="overflow-x-auto">

          <table class="w-full text-sm min-w-[900px]">

            <thead class="bg-slate-50 text-slate-500 text-xs uppercase tracking-wider">
              <tr>
                <th class="text-left px-6 py-4">Judul</th>
                <th class="text-left px-6 py-4">Tujuan</th>
                <th class="text-left px-6 py-4">Budget</th>
                <th class="text-left px-6 py-4">Status</th>
                <th class="text-left px-6 py-4">Aksi</th>
              </tr>
            </thead>

            <tbody class="divide-y divide-slate-100">

              <% if (trips.length===0) { %>
                <tr>
                  <td colspan="5" class="text-center py-10 text-slate-400">
                    Belum ada data pengajuan
                  </td>
                </tr>
                <% } %>

                  <% trips.forEach(t=> { %>
                    <tr class="hover:bg-slate-50 transition">

                      <td class="px-6 py-4 font-medium text-slate-800">
                        <%= t.title %>
                      </td>

                      <td class="px-6 py-4 text-slate-500">
                        <%= t.destination || '-' %>
                      </td>

                      <td class="px-6 py-4 font-semibold text-slate-700">
                        Rp <%= (t.budget || 0).toLocaleString('id-ID') %>
                      </td>

                      <td class="px-6 py-4">

                        <span class="
                    text-xs px-3 py-1 rounded-md font-medium border
                    <%= t.status === 'APPROVED'
                      ? 'bg-emerald-50 text-emerald-600 border-emerald-200'
                      : t.status === 'REJECTED'
                        ? 'bg-red-50 text-red-600 border-red-200'
                        : 'bg-blue-50 text-blue-600 border-blue-200'
                    %>
                  ">
                          <%= t.status %>
                        </span>

                      </td>

                      <td class="px-6 py-4 flex gap-2">

                        <!-- DETAIL (FULL PAGE) -->
                        <a href="/trip/<%= t._id %>"
                          class="px-3 py-1.5 text-xs rounded-lg border border-slate-300 text-slate-600 hover:bg-slate-100">
                          Detail
                        </a>

                        <% if (t.status==='PENDING' ) { %>
                          <a href="/trip/<%= t._id %>/edit"
                            class="px-3 py-1.5 text-xs rounded-lg bg-amber-400 text-white hover:bg-amber-500">
                            Edit
                          </a>
                          <% } %>

                            <% if (t.status==='REJECTED' ) { %>
                              <a href="/trip/<%= t._id %>/edit"
                                class="px-3 py-1.5 text-xs rounded-lg bg-emerald-500 text-white hover:bg-emerald-600">
                                Resubmit
                              </a>
                              <% } %>

                      </td>

                    </tr>
                    <% }) %>

            </tbody>

          </table>

        </div>
      </div>

    </div>
  </div>