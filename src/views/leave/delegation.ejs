<%- layout('layouts/main', { title }) %>
  <div class="ml-72 min-h-screen bg-slate-50/50 p-8 antialiased">
    <div class="max-w-5xl mx-auto">
      <h1 class="text-xl font-bold text-slate-900 mb-2">Permintaan Delegasi Pekerjaan</h1>
      <p class="text-xs text-slate-500 mb-6">Konfirmasi kesediaan Anda untuk mem-backup tugas rekan kerja yang cuti</p>

      <div class="bg-white border border-slate-200 rounded-2xl shadow-sm overflow-hidden">
        <div class="overflow-x-auto">
          <table class="w-full text-left border-collapse">
            <thead>
              <tr class="bg-slate-50 border-b border-slate-200 text-[11px] font-bold text-slate-400 uppercase tracking-wider">
                <th class="py-4 px-6">Rekan Kerja</th>
                <th class="py-4 px-6">Jenis Cuti</th>
                <th class="py-4 px-6">Durasi & Tanggal</th>
                <th class="py-4 px-6">Status Anda</th>
                <th class="py-4 px-6 text-right">Aksi</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-slate-100 text-sm">
              <% if (delegations.length === 0) { %>
                <tr>
                  <td colspan="5" class="py-12 text-center text-slate-400 italic text-xs">
                    Tidak ada permintaan delegasi aktif saat ini
                  </td>
                </tr>
              <% } %>
              
              <% delegations.forEach(del => { %>
                <tr class="hover:bg-slate-50/50 transition-colors">
                  <!-- Kolom Rekan Kerja -->
                  <td class="py-4 px-6 text-slate-900 font-medium">
                    <%= (del.leaveId && del.leaveId.userId && del.leaveId.userId.employeeData) ? del.leaveId.userId.employeeData.fullName : '-' %>
                  </td>
                  
                  <!-- Kolom Jenis Cuti -->
                  <td class="py-4 px-6 font-medium text-slate-600">
                    <%= (del.leaveId && del.leaveId.leaveTypeId) ? del.leaveId.leaveTypeId.name : '-' %>
                  </td>
                  
                  <!-- Kolom Durasi & Tanggal (Formatted) -->
                  <td class="py-4 px-6 text-xs text-slate-500">
                    <span class="font-semibold text-slate-700 block mb-0.5">
                      <%= del.leaveId ? del.leaveId.totalDays : 0 %> Hari Kerja
                    </span>
                    <% if (del.leaveId && del.leaveId.startDate && del.leaveId.endDate) { %>
                      <%= new Date(del.leaveId.startDate).toLocaleDateString('id-ID', {day: '2-digit', month: 'short'}) %>
                      s.d 
                      <%= new Date(del.leaveId.endDate).toLocaleDateString('id-ID', {day: '2-digit', month: 'short', year: 'numeric'}) %>
                    <% } else { %>
                      -
                    <% } %>
                  </td>
                  
                  <!-- Kolom Status Badge -->
                  <td class="py-4 px-6">
                    <span class="text-[11px] font-semibold tracking-wide px-2.5 py-1 rounded-full inline-block
                      <%= del.status === 'APPROVED' ? 'bg-emerald-50 text-emerald-700 border border-emerald-100' : 
                          del.status === 'REJECTED' ? 'bg-rose-50 text-rose-700 border border-rose-100' : 
                          'bg-amber-50 text-amber-700 border border-amber-100' %>">
                      <%= del.status %>
                    </span>
                  </td>
                  
                  <!-- Kolom Aksi Dinamis -->
                  <td class="py-4 px-6 text-right space-x-1 whitespace-nowrap">
                    <!-- Tombol Detail (Bisa diakses kapan saja) -->
                    <% if (del.leaveId) { %>
                      <a href="/leave/detail/<%= del.leaveId._id %>" 
                         class="bg-slate-100 hover:bg-slate-200 border border-slate-200 text-slate-700 rounded-lg px-2.5 py-1.5 text-xs font-bold inline-block transition shadow-sm">
                        Detail
                      </a>
                    <% } %>

                    <!-- Tombol Persetujuan (Hanya muncul jika status masih PENDING) -->
                    <% if (del.status === 'PENDING') { %>
                      <form action="/leave/delegation/approve/<%= del._id %>" method="POST" class="inline">
                        <button type="submit" class="bg-emerald-600 hover:bg-emerald-700 text-white rounded-lg px-2.5 py-1.5 text-xs font-bold cursor-pointer transition shadow-sm">
                          Terima
                        </button>
                      </form>
                      <form action="/leave/delegation/reject/<%= del._id %>" method="POST" class="inline">
                        <button type="submit" class="bg-white hover:bg-rose-50 text-rose-600 border border-rose-200 rounded-lg px-2.5 py-1.5 text-xs font-bold cursor-pointer transition shadow-sm">
                          Tolak
                        </button>
                      </form>
                    <% } %>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>