<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="icon" type="image/x-icon" href="/images/favicon.ico" />
  <link rel="icon" type="image/png" sizes="32x32" href="/images/favicon-32x32.png" />
  <link rel="icon" type="image/png" sizes="16x16" href="/images/favicon-16x15.png" />
  <link class="apple-touch-icon" href="/images/apple-touch-icon.png" />
  <link rel="manifest" href="/images/site.webmanifest" />

  <title><%= typeof title !== 'undefined' ? title : 'Zafa Tour HRIS' %></title>

  <link rel="stylesheet" href="/css/output.css" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css" />
  <script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
</head>

<body class="min-h-dvh overflow-x-hidden md:bg-[#f8fafc] text-slate-800 antialiased">
  <div class="flex min-h-screen w-full" pb-16 md:pb-0">
    <%- include('../partials/sidebar') %>

    <!-- KODE BARU: Tambahkan bg-slate-50/70 agar semua device punya dasar abu-abu tipis yang mewah -->
    <main class="relative flex h-full flex-1 flex-col overflow-visible bg-slate-50/70 px-4 py-5 pt-6 md:px-8 md:pt-20">
      <div class="absolute top-5 right-4 z-[100] hidden md:right-8 md:block" id="notificationDropdown">
        <button id="notifButton" class="group relative flex h-10 w-10 items-center justify-center rounded-xl border border-slate-200/60 bg-white shadow-xs transition-all duration-200 hover:bg-slate-50 focus:outline-none active:scale-[0.96]">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" class="stroke-[2.2] text-slate-600 transition-colors group-hover:text-slate-900">
            <path stroke-linecap="round" stroke-linejoin="round" d="M14.857 17.082a23.848 23.848 0 005.454-1.31A8.967 8.967 0 0118 9.75v-.7V9A6 6 0 006 9v.75a8.967 8.967 0 01-2.312 6.022c1.733.64 3.56 1.085 5.455 1.31m5.714 0a24.255 24.255 0 01-5.714 0m5.714 0a3 3 0 11-5.714 0"></path>
          </svg>
          <span id="notifBadge" class="absolute top-2 right-2 hidden h-2 w-2 rounded-full bg-amber-500 ring-2 ring-white">
            <span class="absolute inline-flex h-full w-full animate-ping rounded-full bg-amber-400 opacity-75"></span>
          </span>
        </button>

        <div id="notifMenu" class="absolute right-0 z-[100] mt-2.5 hidden w-[calc(100vw-2rem)] origin-top-right flex-col rounded-2xl border border-slate-200/50 bg-white shadow-xl shadow-slate-950/[0.05] transition-all duration-200 sm:w-96">
          <div class="flex items-center justify-between rounded-t-2xl border-b border-slate-100 bg-white px-5 py-4">
            <h1 class="flex items-center gap-2 text-[13px] font-bold tracking-tight text-slate-950">
              Notifikasi
              <span id="notifCountText" class="hidden inline-flex min-w-[18px] items-center justify-center rounded-md bg-blue-600 px-2 py-0.5 text-[10px] font-bold text-white">0</span>
            </h1>
            <button id="markAllReadBtn" class="text-[11px] font-bold text-blue-600 transition-colors hover:text-blue-700">Tandai semua dibaca</button>
          </div>
          <div id="notifContainer" class="max-h-[380px] divide-y divide-slate-50 overflow-y-auto">
            <div class="p-6 text-center text-xs text-slate-400">Tidak ada notifikasi baru.</div>
          </div>
        </div>
      </div>

      <div class="ml-0 min-h-screen flex-1   antialiased md: md:p-8">

        <% if (typeof title !== 'undefined' && title.toLowerCase().includes('dashboard')) { %>
        <div class="mb-6 block rounded-3xl  shadow-[0_4px_20px_rgba(0,0,0,0.015)] md:hidden">
          <div class="flex items-center justify-between mb-5 px-1">
            <div>
              <h1 class="text-xl font-extrabold tracking-tight text-slate-900">Beranda</h1>
              <p class="text-[10px] font-medium text-slate-400 mt-0.5">Halo, <%= typeof user !== 'undefined' ? user.fullName : 'Pegawai' %> 👋</p>
            </div>
            <!-- Opsional: Sisi Kanan bisa diisi icon kecil info atau dibiarkan kosong agar clean -->
            <div class="w-8 h-8 rounded-xl bg-slate-50 border border-slate-200/60 flex items-center justify-center text-slate-400">
              <i class="fa-solid fa-sparkles text-xs text-amber-500"></i>
            </div>
          </div>
          <div class="bg-gradient-to-br from-blue-600 via-indigo-600 to-slate-900 rounded-3xl pt-8 pb-10 px-6 text-white shadow relative overflow-hidden mb-6 min-h-[160px] flex flex-col justify-between">

            <div class="absolute -right-4 -top-10 w-36 h-36 bg-white/10 rounded-full blur-2xl pointer-events-none"></div>
            <div class="absolute -right-10 -bottom-10 w-44 h-44 bg-indigo-500/20 rounded-full blur-xl pointer-events-none"></div>

            <div class="flex justify-between items-start w-full relative z-10 select-none">
              <div class="space-y-0.5">
                <p class="text-[10px] font-bold text-blue-100 uppercase tracking-[0.15em] opacity-90">Status Kehadiran</p>
                <% if (!already) { %>
                <h2 class="text-3xl font-extrabold tracking-tight mt-1 bg-gradient-to-r from-white via-white to-blue-100 bg-clip-text text-transparent tabular-nums">
                  -- : --
                </h2>
                <% } else if (already && !hasCheckedOut) { %>
                <h2 class="text-3xl font-extrabold tracking-tight mt-1 bg-gradient-to-r from-white via-white to-blue-100 bg-clip-text text-transparent tabular-nums">
                  <%= checkInTimeStr %>
                </h2>
                <% } else { %>
                <% 
    const checkOutTimeStr = attendance && attendance.checkOut 
      ? moment(attendance.checkOut).format("HH:mm") 
      : "--:--";
  %>
                <h2 class="text-3xl font-extrabold tracking-tight mt-1 bg-gradient-to-r from-emerald-400 via-emerald-300 to-teal-200 bg-clip-text text-transparent tabular-nums">
                  <%= checkOutTimeStr %>
                </h2>
                <% } %>

              </div>

              <% if (!already) { %>
              <a href="/attendance" class="group/btn relative inline-flex items-center gap-2 h-10 px-4 bg-white text-slate-900 font-bold text-xs rounded-xl shadow-[0_4px_12px_rgba(255,255,255,0.15)] hover:shadow-[0_6px_20px_rgba(255,255,255,0.25)] transition-all duration-200 active:scale-95 outline-none overflow-hidden decoration-none shrink-0">
                <span class="relative flex h-2 w-2 shrink-0">
                  <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-emerald-400 opacity-75"></span>
                  <span class="relative inline-flex rounded-full h-2 w-2 bg-emerald-500"></span>
                </span>
                <i class="fa-solid fa-camera text-[13px] text-slate-500 group-hover/btn:text-slate-800 transition-colors"></i>
                <span class="tracking-tight">Check In</span>
              </a>

              <% } else if (already && !hasCheckedOut) { %>
              <a href="/attendance" class="group/btn relative inline-flex items-center gap-2 h-10 px-4 bg-amber-500  text-white font-bold text-xs rounded-xl shadow-[0_4px_12px_rgba(245,158,11,0.2)] transition-all duration-200 active:scale-95 outline-none overflow-hidden decoration-none shrink-0">
                <span class="tracking-tight">Check Out</span>
              </a>

              <% } else { %>
              <div class="inline-flex items-center gap-2 h-10 px-4 bg-white/10 border border-white/20 text-white/90 font-bold text-xs rounded-xl  shrink-0 shadow-3xs">
                <i class="fa-solid fa-circle-check text-emerald-400 text-[13px]"></i>
                <span class="tracking-tight">Selesai Kerja</span>
              </div>
              <% } %>
            </div>

            <div class="w-full pt-4 relative z-10">
              <div class="flex items-center gap-1.5 text-[11px] text-blue-100/80 font-semibold tracking-wide">
                <i class="fa-regular fa-calendar-check text-xs text-blue-300"></i>
                <span>Jam Kerja: 08:30 - 16:30 WIB</span>
              </div>
            </div>

          </div>
          <h3 class="text-xs font-bold  uppercase tracking-wider mb-6">Featured</h3>
          <div class="grid grid-cols-4 gap-x-2 gap-y-6">



            <!-- CUTI & IZIN (AGREGATOR HUB) -->
            <button onclick="openLeaveMenuMobile()" class="group flex flex-col items-center gap-2 text-center focus:outline-none">
              <div class="flex h-13 w-13 items-center justify-center rounded-2xl bg-indigo-50/70 text-indigo-600 border border-indigo-100/20 shadow-[0_6px_16px_rgba(79,70,229,0.02)] transition-all duration-200 active:scale-95">
                <i class="fa-solid fa-calendar-check text-base"></i>
              </div>
              <span class="text-[11px] font-medium text-slate-600 tracking-tight leading-none">Cuti</span>
            </button>

            <% 
  // List role manajemen yang berwenang menyetujui lembur (Semua Manager Bidang & Wadir)
  const overtimeApproverRoles = ['MANAGER_ADMINISTRASI', 'MANAGER_KEUANGAN', 'MANAGER_HAJI_UMRAH', 'WAKIL_DIREKTUR'];
  
  // Cek apakah user berhak melihat Bottom Sheet Lembur
  const canAccessOvertimeMenu = typeof user !== 'undefined' && overtimeApproverRoles.includes(user.role);
%>

            <% if (canAccessOvertimeMenu) { %>
            <!-- Jika Manajemen: Buka Bottom Sheet -->
            <button onclick="openOvertimeMenuMobile()" class="group flex flex-col items-center gap-2 text-center focus:outline-none">
              <div class="flex h-13 w-13 items-center justify-center rounded-2xl bg-purple-50/70 text-purple-600 border border-purple-100/20 shadow-[0_6px_16px_rgba(139,92,246,0.03)] transition-all duration-200 active:scale-95">
                <i class="fa-solid fa-user-clock text-base"></i>
              </div>
              <span class="text-[11px] font-medium text-slate-600 tracking-tight leading-none">Lembur</span>
            </button>
            <% } else { %>
            <!-- Jika Pegawai Biasa: Langsung lari ke Halaman Riwayat -->
            <a href="/overtime/my" class="group flex flex-col items-center gap-2 text-center">
              <div class="flex h-13 w-13 items-center justify-center rounded-2xl bg-purple-50/70 text-purple-600 border border-purple-100/20 shadow-[0_6px_16px_rgba(139,92,246,0.03)] transition-all duration-200 active:scale-95">
                <i class="fa-solid fa-user-clock text-base"></i>
              </div>
              <span class="text-[11px] font-medium text-slate-600 tracking-tight leading-none">Lembur</span>
            </a>
            <% } %>

            <% if (typeof user !== 'undefined' && user?.role === 'WAKIL_DIREKTUR') { %>
            <button type="button" onclick="openPermitMenuMobile()" class="group flex flex-col items-center gap-2 text-center w-full bg-transparent border-none p-0 cursor-pointer outline-none select-none">
              <div class="flex h-13 w-13 items-center justify-center rounded-2xl bg-indigo-50/70 text-indigo-600 border border-indigo-100/20 shadow-[0_6px_16px_rgba(79,70,229,0.03)] transition-all duration-200 active:scale-95 group-hover:bg-indigo-100/50">
                <i class="fa-solid fa-file-signature text-base"></i>
              </div>
              <span class="text-[11px] font-medium text-slate-600 tracking-tight leading-none">Izin</span>
            </button>
            <% } else { %>
            <a href="/permit/history" class="group flex flex-col items-center gap-2 text-center select-none">
              <div class="flex h-13 w-13 items-center justify-center rounded-2xl bg-indigo-50/70 text-indigo-600 border border-indigo-100/20 shadow-[0_6px_16px_rgba(79,70,229,0.03)] transition-all duration-200 active:scale-95 group-hover:bg-indigo-100/50">
                <i class="fa-solid fa-file-signature text-base"></i>
              </div>
              <span class="text-[11px] font-medium text-slate-600 tracking-tight leading-none">Izin</span>
            </a>
            <% } %>
            <!-- SALES VISIT -->
            <a href="/sales/my" class="group flex flex-col items-center gap-2 text-center">
              <div class="flex h-13 w-13 items-center justify-center rounded-2xl bg-rose-50/70 text-rose-600 border border-rose-100/20 shadow-[0_6px_16px_rgba(244,63,94,0.03)] transition-all duration-200 active:scale-95 group-hover:bg-rose-100/50">
                <i class="fa-solid fa-map-pin text-base"></i>
              </div>
              <span class="text-[11px] font-medium text-slate-600 tracking-tight leading-none">Kunjungan Sales</span>
            </a>

            <!-- JURNAL -->
            <% 
  // Daftarkan role yang berhak masuk langsung ke halaman review
  const reviewerRoles = [
    'MANAGER_ADMINISTRASI', 
    'MANAGER_KEUANGAN', 
    'MANAGER_HAJI_UMRAH', 
    'WAKIL_DIREKTUR', 
    'DIREKTUR_UTAMA'
  ];

  // Tentukan path dinamis berdasarkan role user
  const targetPath = (typeof user !== 'undefined' && reviewerRoles.includes(user.role)) 
    ? '/daily-log/review' 
    : '/daily-log';
%>

            <a href="<%= targetPath %>" class="group flex flex-col items-center gap-2 text-center">
              <div class="flex h-13 w-13 items-center justify-center rounded-2xl bg-teal-50/70 text-teal-600 border border-teal-100/20 shadow-[0_6px_16px_rgba(20,184,166,0.03)] transition-all duration-200 active:scale-95 group-hover:bg-teal-100/50">
                <i class="fa-solid fa-pen-to-square text-base"></i>
              </div>
              <span class="text-[11px] font-medium text-slate-600 tracking-tight leading-none">
                Jurnal
              </span>
            </a>

            <a href="/payroll/my-slip" class="group flex flex-col items-center gap-2 text-center">
              <div class="flex h-13 w-13 items-center justify-center rounded-2xl bg-orange-50/70 text-orange-600 border border-orange-100/20 shadow-[0_6px_16px_rgba(249,115,22,0.03)] transition-all duration-200 active:scale-95 group-hover:bg-orange-100/50">
                <i class="fa-solid fa-receipt text-base"></i>
              </div>
              <span class="text-[11px] font-medium text-slate-600 tracking-tight leading-none">Slip Gaji</span>
            </a>

            <% 
  const isManager = typeof user !== 'undefined' && 
                    ['MANAGER_ADMINISTRASI', 'MANAGER_HAJI_UMRAH', 'MANAGER_KEUANGAN', 'WAKIL_DIREKTUR'].includes(user.role);
%>

            <% if (isManager) { %>
            <button onclick="openExpenseMenuMobile()" class="group flex flex-col items-center gap-2 text-center focus:outline-none">
              <div class="flex h-13 w-13 items-center justify-center rounded-2xl bg-cyan-50/70 text-cyan-600 border border-cyan-100/20 shadow-[0_6px_16px_rgba(6,182,212,0.02)] transition-all duration-200 active:scale-95">
                <i class="fa-solid fa-file-invoice text-base"></i>
              </div>
              <span class="text-[11px] font-medium text-slate-600 tracking-tight leading-none">Klaim Beban</span>
            </button>
            <% } else { %>
            <a href="/expense/my" class="group flex flex-col items-center gap-2 text-center focus:outline-none">
              <div class="flex h-13 w-13 items-center justify-center rounded-2xl bg-cyan-50/70 text-cyan-600 border border-cyan-100/20 shadow-[0_6px_16px_rgba(6,182,212,0.02)] transition-all duration-200 active:scale-95">
                <i class="fa-solid fa-file-invoice text-base"></i>
              </div>
              <span class="text-[11px] font-medium text-slate-600 tracking-tight leading-none">Klaim Beban</span>
            </a>
            <% } %>


            <% 
  // List role yang mendapatkan akses bottom sheet kasbon
  const loanManagerRoles = ['MANAGER_ADMINISTRASI', 'MANAGER_HAJI_UMRAH', 'MANAGER_KEUANGAN'];
  const direksiRoles = ['WAKIL_DIREKTUR', 'DIREKTUR_UTAMA'];
  
  // Apakah user termasuk yang bisa membuka menu Kasbon (Bottom Sheet)?
  const canAccessLoanMenu = typeof user !== 'undefined' && 
                           [...loanManagerRoles, ...direksiRoles].includes(user.role);
%>

            <% if (canAccessLoanMenu) { %>
            <button onclick="openLoanMenuMobile()" class="group flex flex-col items-center gap-2 text-center focus:outline-none">
              <div class="flex h-13 w-13 items-center justify-center rounded-2xl bg-violet-50/70 text-violet-600 border border-violet-100/20 shadow-[0_6px_16px_rgba(109,40,217,0.03)] transition-all duration-200 active:scale-95">
                <i class="fa-solid fa-hand-holding-dollar text-base"></i>
              </div>
              <span class="text-[11px] font-medium text-slate-600 tracking-tight leading-none">Kasbon</span>
            </button>
            <% } else { %>
            <a href="/loans/my" class="group flex flex-col items-center gap-2 text-center">
              <div class="flex h-13 w-13 items-center justify-center rounded-2xl bg-violet-50/70 text-violet-600 border border-violet-100/20 shadow-[0_6px_16px_rgba(109,40,217,0.03)] transition-all duration-200 active:scale-95">
                <i class="fa-solid fa-hand-holding-dollar text-base"></i>
              </div>
              <span class="text-[11px] font-medium text-slate-600 tracking-tight leading-none">Kasbon</span>
            </a>
            <% } %>
          </div>
          <div class="mt-10 px-1">
            <h3 class="text-xs font-bold uppercase tracking-wider mb-6">Management Hub</h3>
            <div class="grid grid-cols-4 gap-3">

              <% if (user.role !== 'PEGAWAI') { %>
              <a href="/employee/" class="group flex flex-col items-center gap-1.5 text-center">
                <div class="flex h-12 w-12 items-center justify-center rounded-xl bg-slate-900 text-white shadow-xs transition-all active:scale-95">
                  <i class="fa-solid fa-users text-sm"></i>
                </div>
                <span class="text-[10px] font-bold text-slate-700 tracking-tight leading-none">Data Pegawai</span>
              </a>
              <% } %>

              <!-- <a href="/management/kpi" class="group flex flex-col items-center gap-1.5 text-center">
                <div class="flex h-12 w-12 items-center justify-center rounded-xl bg-purple-50 text-purple-600 border border-purple-100/30 shadow-xs transition-all active:scale-95">
                  <i class="fa-solid fa-chart-simple text-sm"></i>
                </div>
                <span class="text-[10px] font-bold text-slate-700 tracking-tight leading-none">Lihat KPI</span>
              </a> -->

              <a href="/report/attendance" class="group flex flex-col items-center gap-1.5 text-center">
                <div class="flex h-12 w-12 items-center justify-center rounded-xl bg-blue-50 text-blue-600 border border-blue-100/30 shadow-xs transition-all active:scale-95">
                  <i class="fa-solid fa-calendar-check text-sm"></i>
                </div>
                <span class="text-[10px] font-bold text-slate-700 tracking-tight leading-none">Lap. Absen</span>
              </a>
              <% if (user.role == 'WAKIL_DIREKTUR') { %>

              <a href="/report/overview" class="group flex flex-col items-center gap-1.5 text-center">
                <div class="flex h-12 w-12 items-center justify-center rounded-xl bg-rose-50 text-rose-600 border border-rose-100/30 shadow-xs transition-all active:scale-95">
                  <i class="fa-solid fa-chart-line text-sm"></i>
                </div>
                <span class="text-[10px] font-bold text-slate-700 tracking-tight leading-none">Lap. Lengkap</span>
              </a>
              <% } %>
            </div>
          </div>
          <div class="mt-8 border-t border-slate-100 pt-6 pb-8">
            <div class="flex items-center justify-between mb-5 border-b border-slate-100/80 pb-3">
              <h3 class="text-[13px] font-semibold text-slate-700 tracking-tight">
                Pengumuman Terbaru
              </h3>

              <div class="flex items-center gap-4">
                <a href="/announcement/new" class="inline-flex items-center gap-1 text-[12px] font-medium text-slate-500 hover:text-blue-600 transition-colors">
                  <svg class="w-3.5 h-3.5 stroke-[2]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M12 4.5v15m7.5-7.5h-15" />
                  </svg>
                  <span class="hidden sm:inline">Buat</span>
                </a>

                <a href="/announcement" class="text-[12px] font-medium text-blue-600 hover:text-blue-700 transition-colors">
                  Lihat Semua
                </a>
              </div>
            </div>
            <div class="space-y-3">
              <% if (typeof latestAnnouncements !== 'undefined' && latestAnnouncements.length > 0) { %>
              <% latestAnnouncements.slice(0, 2).forEach(ann => { %>
              <a href="/announcement/<%= ann._id %>" class="flex items-center gap-4 p-4 rounded-2xl border border-slate-200/40 bg-slate-50/40 hover:bg-slate-50 transition">

                <% if (ann.category === 'OFFICIAL') { %>
                <div class="w-11 h-11 rounded-xl bg-blue-50 border border-blue-100/40 flex items-center justify-center text-blue-600 shrink-0">
                  <i class="fa-solid fa-bullhorn text-sm"></i>
                </div>
                <% } else { %>
                <div class="w-11 h-11 rounded-xl bg-slate-50 border border-slate-200/50 flex items-center justify-center text-slate-500 shrink-0">
                  <i class="fa-solid fa-leaf text-sm text-emerald-600"></i>
                </div>
                <% } %>

                <div class="min-w-0 flex-1 space-y-0.5">
                  <p class="text-xs font-bold text-slate-800 truncate tracking-tight"><%= ann.title %></p>

                  <!-- DESKRIPSI ISI PENGUMUMAN DENGAN PEMBATASAN SATU BARIS -->
                  <p class="text-[11px] text-slate-400 font-normal line-clamp-1 leading-tight">
                    <%= ann.content %>
                  </p>

                  <p class="text-[10px] text-slate-400/80 font-medium tracking-wide pt-0.5">
                    <%= ann.category === 'OFFICIAL' ? 'Resmi' : 'Umum' %> •
                    <%= new Date(ann.createdAt).toLocaleDateString('id-ID', { day: 'numeric', month: 'short', year: 'numeric' }) %>
                  </p>
                </div>
              </a>
              <% }) %>
              <% } else { %>
              <div class="p-5 text-center text-xs text-slate-400 bg-slate-50/50 rounded-2xl border border-slate-200/20">
                Belum ada pengumuman terbaru.
              </div>
              <% } %>
            </div>
          </div>

        </div>
        <% } %>

        <% if (typeof title !== 'undefined' && !title.toLowerCase().includes('dashboard')) { %>

        <div class="fixed top-0 inset-x-0 z-50 md:hidden bg-white/95 border-b border-slate-200/60">
          <div class="flex items-center gap-3 px-4 h-16">

            <a href="/dashboard" class="flex h-9 w-9 items-center justify-center rounded-xl border border-slate-200 bg-white text-slate-500 active:scale-95">
              <i class="fa-solid fa-chevron-left text-xs"></i>
            </a>

            <div class="min-w-0">
              <h1 class="truncate text-[15px] font-semibold text-slate-900">
                <%= title %>
              </h1>

              <p class="text-[11px] text-slate-400">
                Zafa Tour HRIS
              </p>
            </div>

          </div>
        </div>

        <div class="pt-16 md:pt-1">
          <%- body -%>
        </div>

        <% } else { %>

        <%- body -%>

        <% } %>

      </div>
  </div>
  </main>
  </div>
  <div class="fixed inset-x-0 bottom-0 z-[105] flex items-center justify-around border-t border-white bg-white/70 backdrop-blur-lg px-2 py-3 shadow-[0_-8px_30px_rgba(0,0,0,0.05)] md:hidden">

    <a href="/dashboard" class="flex flex-1 flex-col items-center gap-1 text-slate-600 active:text-blue-600 transition-all hover:text-blue-600">
      <i class="fa-solid fa-house text-lg"></i>
      <span class="text-[9px] font-bold uppercase tracking-wide">Beranda</span>
    </a>

    <a href="/attendance/history" class="flex flex-1 flex-col items-center gap-1 active:text-blue-600 text-slate-600 transition-all hover:text-blue-600">
      <i class="fa-solid fa-clock-rotate-left text-lg"></i>
      <span class="text-[9px] font-bold uppercase tracking-wide">Absensi</span>
    </a>

    <div class="flex-1 flex justify-center relative -top-6">
      <button type="button" onclick="toggleQuickMenuMobile()" class="w-12 h-12 bg-blue-600 text-white rounded-full flex items-center justify-center shadow-lg shadow-blue-500/30 active:scale-90 transition-all duration-300">
        <i id="quickMenuIconMobile" class="fa-solid fa-plus text-xl transition-transform duration-300"></i>
      </button>
    </div>

    <a href="/leave/my-history" class="relative flex flex-1 flex-col items-center gap-1 text-slate-600 transition-all hover:text-blue-600 active:text-blue-600">
      <i class="fa-solid fa-envelope-open-text text-lg"></i>
      <span class="text-[9px] font-bold uppercase tracking-wide">Cuti</span>
    </a>

    <div class="relative flex flex-1 flex-col items-center gap-1 text-slate-600 active:text-blue-600">
      <button type="button" id="accountMenuBtnMobile" onclick="toggleAccountMenuMobile()" class="flex flex-col items-center gap-1 hover:text-blue-600 focus:outline-none">
        <div class="w-6 h-6 rounded-full border-2 border-slate-200 bg-slate-100 flex items-center justify-center overflow-hidden shrink-0 transition-all">
          <% if (typeof user !== 'undefined' && user.foto_profile) { %>
          <img src="<%= user.foto_profile %>" alt="Profil" class="w-full h-full object-cover">
          <% } else { %>
          <span class="text-[10px] font-bold text-slate-600 uppercase"><%= typeof user !== 'undefined' ? user.username.charAt(0) : 'A' %></span>
          <% } %>
        </div>
        <span class="text-[9px] font-bold uppercase tracking-wide">Akun</span>
      </button>

      <div id="accountDropdownMobile" class="hidden absolute bottom-16 right-2 w-44 bg-white/95 backdrop-blur-sm border border-slate-100 rounded-2xl shadow-2xl p-1.5 flex flex-col space-y-0.5 z-[1000]">
        <a href="/employee/detail/<%= typeof user !== 'undefined' ? user?.employeeId : '' %>" class="px-3 py-2.5 text-[11px] font-semibold text-slate-700 hover:bg-slate-50 rounded-xl flex items-center gap-2">
          <i class="fa-solid fa-user text-slate-400"></i> Lihat Informasi
        </a>
        <a href="/change-password" class="px-3 py-2.5 text-[11px] font-semibold text-slate-700 hover:bg-slate-50 rounded-xl flex items-center gap-2">
          <i class="fa-solid fa-user-gear text-slate-400"></i> Ganti Password
        </a>
        <div class="border-t border-slate-100 my-1"></div>
        <a href="/logout" onclick="localStorage.clear();" class="px-3 py-2.5 text-[11px] font-bold text-red-600 hover:bg-red-50 rounded-xl flex items-center gap-2">
          <i class="fa-solid fa-right-from-bracket text-red-400"></i> Keluar
        </a>
      </div>
    </div>
  </div>
  <div id="quickActionMenuMobile" class="fixed inset-0 z-[998] hidden bg-slate-950/30  md:hidden transition-opacity duration-200" onclick="toggleQuickMenuMobile()">
    <div class="absolute bottom-20 inset-x-4 bg-white rounded-3xl p-5 shadow-2xl space-y-3 transform translate-y-10 transition-transform duration-200" onclick="event.stopPropagation()">
      <h3 class="text-[10px] font-bold text-slate-400 uppercase tracking-wider px-1">Buat Pengajuan & Aktivitas</h3>

      <div class="grid grid-cols-4 gap-2">
        <a href="/leave/apply" class="flex flex-col items-center p-2.5 rounded-2xl bg-slate-50 hover:bg-blue-50 border border-slate-100 text-center gap-1.5">
          <div class="w-9 h-9 rounded-xl bg-blue-50 text-blue-600 flex items-center justify-center"><i class="fa-solid fa-plane-departure text-xs"></i></div>
          <span class="text-[9px] font-semibold text-slate-700 leading-tight">Cuti</span>
        </a>

        <a href="/permit/new" class="flex flex-col items-center p-2.5 rounded-2xl bg-slate-50 hover:bg-amber-50 border border-slate-100 text-center gap-1.5">
          <div class="w-9 h-9 rounded-xl bg-amber-50 text-amber-600 flex items-center justify-center"><i class="fa-solid fa-shield-halved text-xs"></i></div>
          <span class="text-[9px] font-semibold text-slate-700 leading-tight">Izin</span>
        </a>

        <a href="/loans/new" class="flex flex-col items-center p-2.5 rounded-2xl bg-slate-50 hover:bg-purple-50 border border-slate-100 text-center gap-1.5">
          <div class="w-9 h-9 rounded-xl bg-purple-50 text-purple-600 flex items-center justify-center"><i class="fa-solid fa-wallet text-xs"></i></div>
          <span class="text-[9px] font-semibold text-slate-700 leading-tight">Kasbon</span>
        </a>


        <a href="/expense/create" class="flex flex-col items-center p-2.5 rounded-2xl bg-slate-50 hover:bg-rose-50 border border-slate-100 text-center gap-1.5">
          <div class="w-9 h-9 rounded-xl bg-rose-50 text-rose-600 flex items-center justify-center"><i class="fa-solid fa-file-invoice-dollar text-xs"></i></div>
          <span class="text-[9px] font-semibold text-slate-700 leading-tight">Klaim Beban</span>
        </a>

        <a href="/sales/new" class="flex flex-col items-center p-2.5 rounded-2xl bg-slate-50 hover:bg-indigo-50 border border-slate-100 text-center gap-1.5">
          <div class="w-9 h-9 rounded-xl bg-indigo-50 text-indigo-600 flex items-center justify-center"><i class="fa-solid fa-map-location-dot text-xs"></i></div>
          <span class="text-[9px] font-semibold text-slate-700 leading-tight">Sales Visit</span>
        </a>

        <a href="/overtime/new" class="flex flex-col items-center p-2.5 rounded-2xl bg-slate-50 hover:bg-orange-50 border border-slate-100 text-center gap-1.5">
          <div class="w-9 h-9 rounded-xl bg-orange-50 text-orange-600 flex items-center justify-center"><i class="fa-solid fa-cloud-moon text-xs"></i></div>
          <span class="text-[9px] font-semibold text-slate-700 leading-tight">Lembur</span>
        </a>
      </div>
    </div>
  </div>

  <div id="confirmModal" class="fixed inset-0 z-[9999] hidden items-center justify-center bg-slate-950/50 p-4 ">
    <div class="w-full max-w-md overflow-hidden rounded-3xl bg-white shadow-2xl">
      <div class="relative border-b border-slate-100 px-6 py-5">
        <div class="absolute inset-x-0 top-0 h-1 bg-gradient-to-r from-amber-500 via-orange-500 to-red-500"></div>
        <div class="flex items-start gap-4">
          <div id="confirmModalIcon" class="flex h-14 w-14 shrink-0 items-center justify-center rounded-2xl bg-amber-50">
            <i class="fas fa-triangle-exclamation text-xl text-amber-600"></i>
          </div>
          <div class="flex-1">
            <h3 id="confirmModalTitle" class="text-lg font-semibold text-slate-900">Konfirmasi Tindakan</h3>
            <p class="mt-1 text-sm text-slate-500">Mohon periksa kembali sebelum melanjutkan.</p>
          </div>
        </div>
      </div>
      <div class="px-6 py-5">
        <p id="confirmModalMessage" class="text-sm leading-relaxed text-slate-600">Apakah Anda yakin ingin melanjutkan tindakan ini?</p>
      </div>
      <div class="flex items-center justify-end gap-3 border-t border-slate-100 bg-slate-50/70 px-6 py-4">
        <button type="button" onclick="closeConfirmModal()" class="rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm font-medium text-slate-700 transition hover:bg-slate-50">Batal</button>
        <button type="button" id="confirmModalButton" class="rounded-xl bg-red-600 px-4 py-2.5 text-sm font-medium text-white transition hover:bg-red-700">Ya, Lanjutkan</button>
      </div>
    </div>
  </div>
  <div id="riwayatMenuMobile" class="fixed inset-0 z-[9999] hidden bg-slate-950/30  md:hidden transition-opacity duration-200" onclick="toggleRiwayatMenuMobile()">
    <div class="absolute bottom-0 inset-x-0 bg-white rounded-t-3xl p-6 shadow-2xl space-y-4 transform translate-y-full transition-transform duration-200" onclick="event.stopPropagation()">
      <div class="w-12 h-1 bg-slate-200 rounded-full mx-auto mb-2" onclick="toggleRiwayatMenuMobile()"></div>
      <h3 class="text-xs font-bold text-slate-400 uppercase tracking-wider text-center">Pilih Riwayat Pengajuan</h3>

      <div class="grid grid-cols-2 gap-3 pt-2">
        <!-- Ubah href dibawah ini sesuai rute page riwayat asli milikmu -->
        <a href="/leave/apply" class="flex flex-col items-center p-4 rounded-2xl bg-slate-50 hover:bg-blue-50 border border-slate-100 text-center gap-2">
          <div class="w-10 h-10 rounded-xl bg-blue-50 text-blue-600 flex items-center justify-center"><i class="fa-solid fa-clock-rotate-left text-sm"></i></div>
          <span class="text-xs font-bold text-slate-700">Riwayat Cuti</span>
        </a>
        <a href="/permit/history" class="flex flex-col items-center p-4 rounded-2xl bg-slate-50 hover:bg-amber-50 border border-slate-100 text-center gap-2">
          <div class="w-10 h-10 rounded-xl bg-amber-50 text-amber-600 flex items-center justify-center"><i class="fa-solid fa-clock-rotate-left text-sm"></i></div>
          <span class="text-xs font-bold text-slate-700">Riwayat Izin</span>
        </a>
      </div>
    </div>
  </div>
  <div id="expenseMenuMobile" class="fixed inset-0 z-[9999] hidden bg-slate-950/30  md:hidden transition-opacity duration-200" onclick="toggleExpenseMenuMobile()">
    <div class="absolute bottom-0 inset-x-0 bg-white rounded-t-3xl p-6 shadow-2xl space-y-4 transform translate-y-full transition-transform duration-200" onclick="event.stopPropagation()">

      <div class="w-12 h-1 bg-slate-200 rounded-full mx-auto mb-2" onclick="toggleExpenseMenuMobile()"></div>

      <h3 class="text-xs font-bold text-slate-400 uppercase tracking-wider text-center">Menu Klaim & Keuangan</h3>

      <div class="grid grid-cols-2 gap-3 pt-2">

        <a href="/expense/create" class="flex flex-col items-center p-4 rounded-2xl bg-slate-50 hover:bg-cyan-50 border border-slate-100 text-center gap-2">
          <div class="w-10 h-10 rounded-xl bg-cyan-50 text-cyan-600 flex items-center justify-center">
            <i class="fa-solid fa-file-circle-plus text-sm"></i>
          </div>
          <span class="text-xs font-bold text-slate-700">Buat Klaim</span>
        </a>

        <a href="/expense/my" class="flex flex-col items-center p-4 rounded-2xl bg-slate-50 hover:bg-blue-50 border border-slate-100 text-center gap-2">
          <div class="w-10 h-10 rounded-xl bg-blue-50 text-blue-600 flex items-center justify-center">
            <i class="fa-solid fa-clock-rotate-left text-sm"></i>
          </div>
          <span class="text-xs font-bold text-slate-700">Riwayat Saya</span>
        </a>

        <% if (typeof user !== 'undefined' && user.role) { %>

        <% 
          const approverRoles = [
            'MANAGER_ADMINISTRASI', 
            'MANAGER_HAJI_UMRAH', 
            'MANAGER_KEUANGAN', 
            'WAKIL_DIREKTUR'
          ];
          if (approverRoles.includes(user.role)) { 
        %>
        <a href="/expense/approval/manager" class="col-span-2 flex items-center gap-3 p-4 rounded-2xl bg-amber-50/60 hover:bg-amber-50 border border-amber-100 text-left">
          <div class="w-10 h-10 rounded-xl bg-amber-500 text-white flex items-center justify-center shrink-0 shadow-sm">
            <i class="fa-solid fa-user-shield text-sm"></i>
          </div>
          <div>
            <span class="text-xs font-bold text-slate-800 block">Persetujuan Klaim</span>
            <span class="text-[10px] text-slate-500 font-medium">Validasi berkas klaim pending milik staf</span>
          </div>
        </a>
        <% } %>

        <% if (user.role === 'MANAGER_KEUANGAN') { %>
        <a href="/expense/finance" class="col-span-2 flex items-center gap-3 p-4 rounded-2xl bg-emerald-50/60 hover:bg-emerald-50 border border-emerald-100 text-left">
          <div class="w-10 h-10 rounded-xl bg-emerald-600 text-white flex items-center justify-center shrink-0 shadow-sm">
            <i class="fa-solid fa-money-bill-transfer text-sm"></i>
          </div>
          <div>
            <span class="text-xs font-bold text-slate-800 block">Validasi & Pencairan</span>
            <span class="text-[10px] text-slate-500 font-medium">Transfer dana & upload bukti transfer resmi</span>
          </div>
        </a>
        <% } %>

        <% } %>

      </div>
    </div>
  </div>
  <div id="loanMenuMobile" class="fixed inset-0 z-[9999] hidden bg-slate-950/30  md:hidden transition-opacity duration-200" onclick="toggleLoanMenuMobile()">
    <div class="absolute bottom-0 inset-x-0 bg-white rounded-t-3xl p-6 shadow-2xl space-y-4 transform translate-y-full transition-transform duration-200" onclick="event.stopPropagation()">
      <div class="w-12 h-1 bg-slate-200 rounded-full mx-auto mb-2" onclick="toggleLoanMenuMobile()"></div>
      <h3 class="text-xs font-bold text-slate-400 uppercase tracking-wider text-center">Menu Kasbon</h3>
      <div class="grid grid-cols-2 gap-3 pt-2">
        <a href="/loans/new" class="flex flex-col items-center p-4 rounded-2xl bg-slate-50 hover:bg-violet-50 border border-slate-100 text-center gap-2">
          <div class="w-10 h-10 rounded-xl bg-violet-50 text-violet-600 flex items-center justify-center"><i class="fa-solid fa-plus text-sm"></i></div>
          <span class="text-xs font-bold text-slate-700">Buat Kasbon</span>
        </a>

        <a href="/loans/my" class="flex flex-col items-center p-4 rounded-2xl bg-slate-50 hover:bg-violet-50 border border-slate-100 text-center gap-2">
          <div class="w-10 h-10 rounded-xl bg-violet-50 text-violet-600 flex items-center justify-center"><i class="fa-solid fa-clock-rotate-left text-sm"></i></div>
          <span class="text-xs font-bold text-slate-700">Riwayat Saya</span>
        </a>

        <% if (typeof user !== 'undefined' && ['WAKIL_DIREKTUR', 'DIREKTUR_UTAMA'].includes(user.role)) { %>
        <a href="/loans/approval" class="col-span-2 flex items-center gap-3 p-4 rounded-2xl bg-amber-50/60 hover:bg-amber-50 border border-amber-100 text-left">
          <div class="w-10 h-10 rounded-xl bg-amber-500 text-white flex items-center justify-center shrink-0 shadow-sm">
            <i class="fa-solid fa-user-shield text-sm"></i>
          </div>
          <div>
            <span class="text-xs font-bold text-slate-800 block">Persetujuan Kasbon</span>
            <span class="text-[10px] text-slate-500 font-medium">Validasi pengajuan kasbon staf & manager</span>
          </div>
        </a>
        <% } %>
      </div>
    </div>
  </div>
  <!-- BOTTOM SHEET MENU LEMBUR MOBILE -->
  <div id="overtimeMenuMobile" class="fixed inset-0 z-[9999] hidden bg-slate-950/30  md:hidden transition-opacity duration-200" onclick="toggleOvertimeMenuMobile()">
    <div class="absolute bottom-0 inset-x-0 bg-white rounded-t-3xl p-6 shadow-2xl space-y-4 transform translate-y-full transition-transform duration-200" onclick="event.stopPropagation()">

      <!-- Handle Bar Modal -->
      <div class="w-12 h-1 bg-slate-200 rounded-full mx-auto mb-2" onclick="toggleOvertimeMenuMobile()"></div>

      <h3 class="text-xs font-bold text-slate-400 uppercase tracking-wider text-center">Menu Dinas Lembur</h3>

      <div class="grid grid-cols-2 gap-3 pt-2">
        <!-- Tombol Catat Lembur Baru -->
        <a href="/overtime/new" class="flex flex-col items-center p-4 rounded-2xl bg-slate-50 hover:bg-purple-50 border border-slate-100 text-center gap-2">
          <div class="w-10 h-10 rounded-xl bg-purple-50 text-purple-600 flex items-center justify-center">
            <i class="fa-solid fa-plus text-sm"></i>
          </div>
          <span class="text-xs font-bold text-slate-700">Catat Lembur</span>
        </a>

        <!-- Tombol Riwayat Lembur Sendiri -->
        <a href="/overtime/my" class="flex flex-col items-center p-4 rounded-2xl bg-slate-50 hover:bg-purple-50 border border-slate-100 text-center gap-2">
          <div class="w-10 h-10 rounded-xl bg-purple-50 text-purple-600 flex items-center justify-center">
            <i class="fa-solid fa-clock-rotate-left text-sm"></i>
          </div>
          <span class="text-xs font-bold text-slate-700">Riwayat Saya</span>
        </a>

        <!-- Tombol Khusus Menu Persetujuan (Akan Muncul Penuh di baris bawah) -->
        <a href="/overtime/approval" class="col-span-2 flex items-center gap-3 p-4 rounded-2xl bg-amber-50/60 hover:bg-amber-50 border border-amber-100 text-left">
          <div class="w-10 h-10 rounded-xl bg-amber-500 text-white flex items-center justify-center shrink-0 shadow-sm">
            <i class="fa-solid fa-user-shield text-sm"></i>
          </div>
          <div>
            <span class="text-xs font-bold text-slate-800 block">Persetujuan Lembur</span>
            <span class="text-[10px] text-slate-500 font-medium">Validasi dan akumulasi jam lembur aktual staf</span>
          </div>
        </a>
      </div>

    </div>
  </div>
  <div id="leaveMenuMobile" class="fixed inset-0 z-[9999] hidden bg-slate-950/30 md:hidden transition-opacity duration-200" onclick="toggleLeaveMenuMobile()">
    <div class="absolute bottom-0 inset-x-0 bg-white rounded-t-3xl p-6 shadow-2xl space-y-4 transform translate-y-full transition-transform duration-200" onclick="event.stopPropagation()">

      <div class="w-12 h-1 bg-slate-200 rounded-full mx-auto mb-2" onclick="toggleLeaveMenuMobile()"></div>

      <h3 class="text-xs font-bold text-slate-400 uppercase tracking-wider text-center">Menu Pengajuan Cuti</h3>

      <div class="grid grid-cols-2 gap-3 pt-2">

        <% if (typeof user !== 'undefined' && user?.permissions?.leave_new) { %>
        <a href="/leave/apply" class="flex flex-col items-center p-4 rounded-2xl bg-slate-50 hover:bg-indigo-50 border border-slate-100 text-center gap-2 transition-colors">
          <div class="w-10 h-10 rounded-xl bg-indigo-50 text-indigo-600 flex items-center justify-center">
            <i class="fa-solid fa-calendar-plus text-sm"></i>
          </div>
          <span class="text-xs font-bold text-slate-700">Ajukan Cuti</span>
        </a>
        <% } %>

        <% if (typeof user !== 'undefined' && user?.permissions?.leave_my) { %>
        <a href="/leave/my-history" class="flex flex-col items-center p-4 rounded-2xl bg-slate-50 hover:bg-indigo-50 border border-slate-100 text-center gap-2 transition-colors">
          <div class="w-10 h-10 rounded-xl bg-indigo-50 text-indigo-600 flex items-center justify-center">
            <i class="fa-solid fa-clock-rotate-left text-sm"></i>
          </div>
          <span class="text-xs font-bold text-slate-700">Riwayat Saya</span>
        </a>
        <% } %>

        <% if (typeof user !== 'undefined' && user?.permissions?.leave_delegation) { %>
        <a href="/leave/my-delegations" class="col-span-2 flex items-center gap-3 p-4 rounded-2xl bg-slate-50 hover:bg-indigo-50 border border-slate-100 text-left transition-colors">
          <div class="w-10 h-10 rounded-xl bg-indigo-100/50 text-indigo-700 flex items-center justify-center shrink-0">
            <i class="fa-solid fa-hands-sharing text-sm"></i>
          </div>
          <div>
            <span class="text-xs font-bold text-slate-800 block">Permintaan Delegasi</span>
            <span class="text-[10.5px] text-slate-500 font-medium leading-none">Konfirmasi pengalihan tugas dari rekan kerja</span>
          </div>
        </a>
        <% } %>

        <% if (typeof user !== 'undefined' && user?.permissions?.leave_manage) { %>
        <a href="/leave/manage-requests" class="col-span-2 flex items-center gap-3 p-4 rounded-2xl bg-amber-50/60 hover:bg-amber-50 border border-amber-100 text-left transition-colors">
          <div class="w-10 h-10 rounded-xl bg-amber-500 text-white flex items-center justify-center shrink-0 shadow-sm">
            <i class="fa-solid fa-user-shield text-sm"></i>
          </div>
          <div>
            <span class="text-xs font-bold text-slate-800 block">Manajemen & Persetujuan Cuti</span>
            <span class="text-[10.5px] text-slate-500 font-medium leading-none">Validasi kuota harian dan verifikasi dokumen pengajuan staf</span>
          </div>
        </a>
        <% } %>
      </div>

    </div>
  </div>
  <div id="permitMenuMobile" class="fixed inset-0 z-[9999] hidden bg-slate-950/30 md:hidden transition-opacity duration-200 opacity-0" onclick="togglePermitMenuMobile()">
    <div class="absolute bottom-0 inset-x-0 bg-white rounded-t-3xl p-6 shadow-2xl space-y-4 transform translate-y-full transition-transform duration-200" onclick="event.stopPropagation()">

      <div class="w-12 h-1 bg-slate-200 rounded-full mx-auto mb-2" onclick="togglePermitMenuMobile()"></div>

      <h3 class="text-xs font-bold text-slate-400 uppercase tracking-wider text-center">Menu Administrasi Izin</h3>

      <div class="grid grid-cols-2 gap-3 pt-2">

        <% if (typeof user !== 'undefined' && user?.permissions?.permit_new) { %>
        <a href="/permit/new" class="flex flex-col items-center p-4 rounded-2xl bg-slate-50 hover:bg-indigo-50 border border-slate-100 text-center gap-2 transition-colors">
          <div class="w-10 h-10 rounded-xl bg-indigo-50 text-indigo-600 flex items-center justify-center">
            <i class="fa-solid fa-file-circle-plus text-sm"></i>
          </div>
          <span class="text-xs font-bold text-slate-700">Buat Pengajuan</span>
        </a>
        <% } %>

        <% if (typeof user !== 'undefined' && user?.permissions?.permit_my) { %>
        <a href="/permit/history" class="flex flex-col items-center p-4 rounded-2xl bg-slate-50 hover:bg-indigo-50 border border-slate-100 text-center gap-2 transition-colors">
          <div class="w-10 h-10 rounded-xl bg-indigo-50 text-indigo-600 flex items-center justify-center">
            <i class="fa-solid fa-clock-rotate-left text-sm"></i>
          </div>
          <span class="text-xs font-bold text-slate-700">Riwayat Pengajuan</span>
        </a>
        <% } %>

        <% if (typeof user !== 'undefined' && user?.role === 'WAKIL_DIREKTUR') { %>
        <a href="/permit/incoming" class="col-span-2 flex items-center gap-3 p-4 rounded-2xl bg-amber-50/60 hover:bg-amber-50 border border-amber-100 text-left transition-colors">
          <div class="w-10 h-10 rounded-xl bg-amber-500 text-white flex items-center justify-center shrink-0 shadow-sm">
            <i class="fa-solid fa-user-shield text-sm"></i>
          </div>
          <div>
            <span class="text-xs font-bold text-slate-800 block">Persidangan & Persetujuan Izin</span>
            <span class="text-[10.5px] text-slate-500 font-medium leading-tight block mt-0.5">Otorisasi dokumen dinas luar, penugasan, dan verifikasi berkas staf</span>
          </div>
        </a>
        <% } %>
      </div>

    </div>
  </div>


  <div id="toastContainer" class="pointer-events-auto fixed top-5 right-5 z-[99999] flex flex-col gap-3.5"></div>

  <script src="/js/custom-modal.js"></script>
  <script src="/js/sidebar-handler.js"></script>
  <script src="/js/notification-handler.js"></script>

  <script>
    function openRiwayatMenuMobile() {
      const menu = document.getElementById('riwayatMenuMobile');
      menu.classList.remove('hidden');
      setTimeout(() => {
        menu.querySelector('.transform').classList.remove('translate-y-full');
      }, 10);
    }

    function toggleRiwayatMenuMobile() {
      const menu = document.getElementById('riwayatMenuMobile');
      menu.querySelector('.transform').classList.add('translate-y-full');
      setTimeout(() => {
        menu.classList.add('hidden');
      }, 200);
    }


    function toggleQuickMenuMobile() {
      const menu = document.getElementById('quickActionMenuMobile');
      const icon = document.getElementById('quickMenuIconMobile');
      document.getElementById('accountDropdownMobile').classList.add('hidden');
      if (menu.classList.contains('hidden')) {
        menu.classList.remove('hidden');
        setTimeout(() => menu.querySelector('div').classList.remove('translate-y-10'), 10);
        icon.classList.add('rotate-45');
      } else {
        menu.querySelector('div').classList.add('translate-y-10');
        setTimeout(() => menu.classList.add('hidden'), 150);
        icon.classList.remove('rotate-45');
      }
    }

    function toggleAccountMenuMobile() {
      const dropdown = document.getElementById('accountDropdownMobile');
      if (dropdown) {
        dropdown.classList.toggle('hidden');
      }
    }

    document.addEventListener('click', function(e) {
      const dropdown = document.getElementById('accountDropdownMobile');

      // PERBAIKAN UTAMA: Mencari elemen terdekat ke atas yang memiliki ID tombol akun
      // Ini membuat klik pada gambar/avatar di dalam tombol tetap terhitung sebagai mengklik tombol
      const isClickInsideButton = e.target.closest('#accountMenuBtnMobile');

      // Jika dropdown sedang terbuka, dan yang diklik BUKAN area dalam dropdown, DAN BUKAN tombolnya, baru tutup menu.
      if (dropdown && !dropdown.contains(e.target) && !isClickInsideButton) {
        dropdown.classList.add('hidden');
      }
    });

    if (document.getElementById('liveClockMobile')) {
      setInterval(() => {
        const now = new Date();
        document.getElementById('liveClockMobile').textContent = now.toLocaleTimeString('id-ID', {
          hour12: false
        });
      }, 1000);
    }

    function triggerGlobalToast({
      title = "Info",
      message = "",
      type = "success"
    }) {
      const container = document.getElementById("toastContainer");
      if (!container) return;
      const styles = {
        success: {
          icon: "fa-circle-check",
          iconColor: "text-emerald-500",
          accentBorder: "border-l-emerald-500"
        },
        error: {
          icon: "fa-circle-xmark",
          iconColor: "text-rose-500",
          accentBorder: "border-l-rose-500"
        }
      };
      const s = styles[type] || styles.success;
      const toast = document.createElement("div");
      toast.className = "w-[360px] rounded-xl border border-slate-200/60 border-l-4 " + s.accentBorder + " bg-white/95  shadow-md p-4 transform translate-x-20 opacity-0 transition-all duration-300 ease-out";
      toast.innerHTML =
        '<div class="flex items-start gap-3">' +
        '<div class="flex-shrink-0 mt-0.5"><i class="fa-solid ' + s.icon + ' ' + s.iconColor + ' text-lg"></i></div>' +
        '<div class="flex-1 min-w-0 text-left">' +
        '<h4 class="text-[13px] font-bold text-slate-900 tracking-tight">' + title + '</h4>' +
        '<p class="mt-0.5 text-xs font-medium text-slate-500 leading-relaxed break-words">' + message + '</p>' +
        '</div>' +
        '<button class="text-slate-400 hover:text-slate-600 transition-colors flex-shrink-0 p-0.5 hover:bg-slate-100 rounded-lg"><i class="fa-solid fa-xmark text-xs"></i></button>' +
        '</div>';
      container.appendChild(toast);
      setTimeout(() => {
        toast.classList.remove("translate-x-20", "opacity-0");
        toast.classList.add("translate-x-0", "opacity-100");
      }, 50);
      const closeToast = () => {
        toast.classList.remove("translate-x-0", "opacity-100");
        toast.classList.add("translate-x-10", "opacity-0");
        setTimeout(() => toast.remove(), 300);
      };
      toast.querySelector("button").addEventListener("click", closeToast);
      setTimeout(closeToast, 4000);
    }

    function openExpenseMenuMobile() {
      const menu = document.getElementById('expenseMenuMobile');
      menu.classList.remove('hidden');
      setTimeout(() => {
        menu.querySelector('.transform').classList.remove('translate-y-full');
      }, 10);
    }

    function toggleExpenseMenuMobile() {
      const menu = document.getElementById('expenseMenuMobile');
      menu.querySelector('.transform').classList.add('translate-y-full');
      setTimeout(() => {
        menu.classList.add('hidden');
      }, 200);
    }

    function openLoanMenuMobile() {
      const menu = document.getElementById('loanMenuMobile');
      menu.classList.remove('hidden');
      setTimeout(() => menu.querySelector('.transform').classList.remove('translate-y-full'), 10);
    }

    function toggleLoanMenuMobile() {
      const menu = document.getElementById('loanMenuMobile');
      menu.querySelector('.transform').classList.add('translate-y-full');
      setTimeout(() => menu.classList.add('hidden'), 200);
    }

    function openOvertimeMenuMobile() {
      const menu = document.getElementById('overtimeMenuMobile');
      menu.classList.remove('hidden');
      setTimeout(() => menu.querySelector('.transform').classList.remove('translate-y-full'), 10);
    }

    function toggleOvertimeMenuMobile() {
      const menu = document.getElementById('overtimeMenuMobile');
      menu.querySelector('.transform').classList.add('translate-y-full');
      setTimeout(() => menu.classList.add('hidden'), 200);
    }

    function openLeaveMenuMobile() {
      const menu = document.getElementById('leaveMenuMobile');
      menu.classList.remove('hidden');
      // Berikan jeda mikro agar transisi transform Tailwind berjalan lancar
      setTimeout(() => menu.querySelector('.transform').classList.remove('translate-y-full'), 10);
    }

    function toggleLeaveMenuMobile() {
      const menu = document.getElementById('leaveMenuMobile');
      menu.querySelector('.transform').classList.add('translate-y-full');
      // Sembunyikan container utama setelah animasi slide-down selesai
      setTimeout(() => menu.classList.add('hidden'), 200);
    }

    function openPermitMenuMobile() {
      const menu = document.getElementById('permitMenuMobile');
      if (!menu) return;

      menu.classList.remove('hidden');
      // Micro-delay untuk memastikan kelancaran transisi CSS opacity dan translate-y dari Tailwind
      setTimeout(() => {
        menu.classList.add('opacity-100');
        menu.querySelector('.transform').classList.remove('translate-y-full');
      }, 10);
    }

    function togglePermitMenuMobile() {
      const menu = document.getElementById('permitMenuMobile');
      if (!menu) return;

      const content = menu.querySelector('.transform');

      // Jika menu sedang terbuka, kita slide-down ke bawah dan sembunyikan
      if (!content.classList.contains('translate-y-full')) {
        content.classList.add('translate-y-full');
        menu.classList.remove('opacity-100');
        setTimeout(() => {
          menu.classList.add('hidden');
        }, 200);
      } else {
        openPermitMenuMobile();
      }
    }
  </script>
</body>

</html>