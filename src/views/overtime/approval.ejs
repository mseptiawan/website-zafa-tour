<% layout('layouts/main') -%>

<div class="ml-72 min-h-screen bg-slate-50 p-6">
  <div class="max-w-6xl mx-auto">

    <!-- HEADER -->
    <div class="mb-6 text-center">
      <h1 class="text-xl font-medium text-slate-800">
        Approval Lembur
      </h1>
      <p class="text-sm text-slate-500 mt-1">
        Kelola persetujuan lembur karyawan
      </p>
    </div>

    <!-- TABLE CARD -->
    <div class="bg-white border border-slate-200 rounded-xl p-4">

      <div class="overflow-x-auto">

        <table class="w-full text-sm">

          <!-- HEADER -->
          <thead>
            <tr class="text-left text-xs text-slate-500 border-b border-slate-200">
              <th class="py-2 px-3 font-normal">Karyawan</th>
              <th class="py-2 px-3 font-normal">Tanggal</th>
              <th class="py-2 px-3 font-normal">Jam</th>
              <th class="py-2 px-3 font-normal">Total</th>
              <th class="py-2 px-3 font-normal">Status</th>
              <th class="py-2 px-3 font-normal text-center">Aksi</th>
            </tr>
          </thead>

          <!-- BODY -->
          <tbody class="divide-y divide-slate-100">

            <% overtimes.forEach(o => { %>
              <tr class="hover:bg-slate-50">

                <!-- KARYAWAN -->
                <td class="py-3 px-3 text-slate-700">
             <%= o.employeeName %>
                </td>

                <!-- TANGGAL -->
                <td class="py-3 px-3 text-slate-600">
                  <%= new Date(o.date).toLocaleDateString("id-ID") %>
                </td>

                <!-- JAM -->
                <td class="py-3 px-3 text-slate-600">
                  <%= o.startTime %> - <%= o.endTime %>
                </td>

                <!-- TOTAL -->
                <td class="py-3 px-3 text-slate-600">
                  <%= o.totalHours %> jam
                </td>

                <!-- STATUS -->
                <td class="py-3 px-3">
                  <span class="text-xs px-2 py-1 rounded bg-slate-100 text-slate-600">
                    <%= o.status %>
                  </span>
                </td>

                <!-- AKSI -->
                <td class="py-3 px-3">
                  <div class="flex justify-center gap-2">

                    <% if(o.status === 'Pending Manager'){ %>
                      <form action="/overtime/approval/<%= o._id %>/manager" method="POST">
                        <button class="text-xs px-3 py-1.5 rounded bg-indigo-600 text-white hover:bg-indigo-700">
                          Approve
                        </button>
                      </form>
                    <% } %>

                  </div>
                </td>

              </tr>
            <% }) %>

          </tbody>

        </table>

      </div>

    </div>

  </div>
</div>
